type UserUIConfiguration = {
    alertWaitTime?: number
}

export const defaultUserConfiguration: UserUIConfiguration = {
    alertWaitTime: 300,
}

export const customUserConfiguration: UserUIConfiguration = {};